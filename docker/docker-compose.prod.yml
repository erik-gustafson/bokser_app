services:
  api:
    container_name: bokser_app_api_prod
    ports:
      - ${PROD_API_HOST_PORT:-8010}:8000
    environment:
      APP_ENV: prod

  worker:
    container_name: bokser_app_worker_prod
    environment:
      APP_ENV: prod

